# OpenAI API Key (Required if using OpenAI provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Azure OpenAI Secrets (Required if using Azure provider)
# NOTE: Endpoint is configured in config.yaml. Only Keys/Tokens go here.
AZURE_OPENAI_API_KEY=your-azure-key
# OR
AZURE_OPENAI_AD_TOKEN=your-bearer-token

# Database URL
DATABASE_URL=postgresql://user:password@db:5432/vox
